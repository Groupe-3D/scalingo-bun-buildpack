#!/bin/bash
set -e

# Vérifie si un package.json existe dans le répertoire racine
if [ -f package.json ]; then
  echo "Bun Buildpack"
  exit 0
else
  echo "No package.json found in root directory. Exiting."
  exit 1
fi
